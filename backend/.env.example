# Base de datos (PostgreSQL en Supabase/Railway/Neon)
DATABASE_URL="postgresql://usuario:contraseña@host:5432/toolvexos"

# JWT Secret (genera uno con: openssl rand -base64 32)
JWT_SECRET="tu_secreto_super_seguro_aqui"
JWT_EXPIRES_IN="7d"

# Puerto del servidor
PORT=3001

# OpenAI (para generador de texto con IA)
OPENAI_API_KEY="sk-..."

# CORS - URL del frontend
FRONTEND_URL="http://localhost:5173"

# Entorno
NODE_ENV="development"
