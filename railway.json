{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd toolvexos/backend && npm install && npx prisma generate --schema=src/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "cd toolvexos/backend && node src/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}